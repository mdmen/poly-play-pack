{
  "name": "poly-play-pack",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "with-env": "dotenv -e ./.env --",
    "dev": "pnpm with-env turbo dev",
    "build": "turbo build",
    "prepare": "husky"
  },
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@poly-play-pack/eslint-config": "workspace:*",
    "dotenv-cli": "^8.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3"
  }
}
